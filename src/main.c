#include "scop.h"

int main (int ac, char **av)
{
    if (ac > 1)
    {
        ft_putstr(av[1]);
    }
    ft_putstr("\ni'm in!\n\n");
    while (1)
    {
        ;
    }
    ft_putstr("i'm out!\n");
    return (0);
}
